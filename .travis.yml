language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t devopsheroes22/travis-test2:latest .
  - docker login -u devopsheroes22 --password dockerpassword
  - docker push devopsheroes22/travis-test2:latest

after_success:
 - echo "done"
  
